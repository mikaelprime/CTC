fastapi==0.140.7
uvicorn[standard]==0.51.0

SQLAlchemy==2.0.51

psycopg2-binary==2.9.12

python-dotenv==1.2.2

python-jose[cryptography]==3.5.0

# passlib 1.7.4 lee bcrypt.__about__.__version__ para detectar la version del
# backend; bcrypt>=4.1 elimino ese modulo y passlib lo reporta como
# "(trapped) error reading bcrypt version" en cada arranque (no rompe el
# login, solo ensucia el log). bcrypt 4.0.1 todavia expone __about__.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

python-multipart==0.0.32

pydantic-settings==2.14.2

alembic==1.18.5

email-validator==2.3.0

pytest==8.4.2
pytest-cov==7.0.0
httpx==0.28.1